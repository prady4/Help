int lengthOfLastWord(const string &A) {
    string final = "";
    int len = 0;
    string word;
    istringstream iss(A, istringstream::in);
    while(iss>>word)
    {
            len = word.size();
    }
    return len;
}

